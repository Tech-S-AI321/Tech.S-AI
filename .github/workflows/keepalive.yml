name: Keep Render Alive

on:
  schedule:
    # Runs every 14 minutes, Monday through Sunday
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit the website
        run: curl -I https://tech-s-ai-28r0.onrender.com
